document.querySelector('<%= "#answer-node-#{@answer.id}" %>')
    .insertAdjacentHTML('afterBegin', '<%= render 'shared/errors', resource: @answer %>');

<% if !@answer.errors.present? %>
  document.querySelector('<%= "#answer-node-#{@answer.id}" %>')
    .innerHTML = '<%= j render @answer %>';
<% end %>
